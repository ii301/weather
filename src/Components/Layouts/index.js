import Header from './Header'

// if you have multiple layouts, import and export them here so whenever you want to import a layout, 
// the import statement is cleaner looking in the file that is importing
export {
    Header
}